#include <stdio.h>
    int main () {
        float base, altura, retangulo;
        printf("Qual a base do seu retangulo?");
        scanf("%f",&base);

        printf("Qual a altura do seu retangulo?");
        scanf("%f",&altura);

retangulo = base * altura;

printf("A seu retangulo tem %.2f, de tamanho",retangulo);

return 0; } 